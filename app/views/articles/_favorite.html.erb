<%= form_for(current_author.favorite_articles.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :article_id, @article.id %></div>
    <%= f.submit 'Favorite', class: 'btn btn-primary' %>
<% end %>